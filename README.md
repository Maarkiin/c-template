# C template
 - includes file structuring src/ include/ obj/ res/ build/
 - cross platform makefile (make/mingw32-make.exe):
 -     generates main(.exe), compile_commands.json ( if used with bear for linux )
